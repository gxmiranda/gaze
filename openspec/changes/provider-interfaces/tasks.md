## 1. Interface Definitions

- [x] 1.1 Create `internal/crap/provider.go` with `FunctionComplexity` struct (Package, Function, File, Line, Complexity fields with JSON tags) and four provider interface definitions: `ComplexityProvider` (method `Analyze(patterns []string, rootDir string) ([]FunctionComplexity, error)`), `LineCoverageProvider` (method `Coverage(patterns []string, rootDir string, coverProfile string) ([]FuncCoverage, error)`), `SideEffectAnalyzer` (method `Analyze(pkgPath string) ([]taxonomy.AnalysisResult, error)`), `ContractCoverageProvider` (method `Build(patterns []string, rootDir string) (func(pkg, function string) (ContractCoverageInfo, bool), []string, error)`). File MUST NOT import `go/ast`, `go/types`, `go/packages`, `go/ssa`, or `github.com/fzipp/gocyclo`. Note: `SideEffectAnalyzer` is NOT added to `crap.Options` — it is consumed only by `ContractCoverageProvider` implementations as a composition dependency (see D5).
- [x] 1.2 Add provider fields to `crap.Options` in `internal/crap/crap.go`: `ComplexityProvider ComplexityProvider`, `LineCoverageProvider LineCoverageProvider`, `ContractCoverageProvider ContractCoverageProvider`. Keep existing `ContractCoverageFunc` and `SSADegradedPackages` fields for backward compatibility, adding `// Deprecated:` GoDoc comments pointing to `ContractCoverageProvider`.

## 2. CRAP Pipeline Providers

- [x] 2.1 Create `internal/provider/goprovider/complexity.go` with `GoComplexityProvider` struct implementing `crap.ComplexityProvider`. The `Analyze` method wraps `gocyclo.Analyze()` and converts `[]gocyclo.Stat` to `[]crap.FunctionComplexity`. Move `testFileRegexp` from `crap/analyze.go` into this adapter. The `resolvePatterns` function stays in `crap/analyze.go` as an exported utility (`ResolvePatterns`) shared by both Go providers (see D9). Include a constructor `NewComplexityProvider() *GoComplexityProvider`.
- [x] 2.2 Create `internal/provider/goprovider/coverage.go` with `GoLineCoverageProvider` struct implementing `crap.LineCoverageProvider`. The `Coverage` method wraps `generateCoverProfile()` + `ParseCoverProfile()`. When `coverProfile` is non-empty, skip profile generation and parse the provided file. Move coverage generation logic (including `recoverPartialProfile` for partial failure recovery) from `crap/analyze.go`. The `LineCoverageProvider` returns `[]FuncCoverage`; the consumer (`crap.Analyze`) calls `buildCoverMap` to build lookup structures (see D8). Include fields for `Stderr io.Writer` configuration. Include a constructor `NewLineCoverageProvider(stderr io.Writer) *GoLineCoverageProvider`.
- [x] 2.3 [ATOMIC with 2.4] Refactor `computeScores` in `internal/crap/analyze.go` to accept `[]crap.FunctionComplexity` instead of `[]gocyclo.Stat`. Update field access: `stat.PkgName` to `stat.Package`, `stat.FuncName` to `stat.Function`, `stat.Pos.Filename` to `stat.File`, `stat.Pos.Line` to `stat.Line`. `stat.Complexity` is unchanged. Also update `lookupCoverage` to accept `FunctionComplexity` instead of `gocyclo.Stat` (or inline it into `computeScores`), since it is called by `computeScores` and shares the same parameter type.
- [x] 2.4 [ATOMIC with 2.3] Refactor `crap.Analyze` in `internal/crap/analyze.go` to use `ComplexityProvider` and `LineCoverageProvider` from `Options`. Callers construct providers and pass them via `Options` (see D3). Remove direct `gocyclo` import from `analyze.go`. The `coverMaps` type and `buildCoverMap` MUST stay in `analyze.go` (see D8). Tasks 2.3 and 2.4 MUST be committed together (see D10).
- [x] 2.5 Update `internal/crap/analyze_internal_test.go` to use `crap.FunctionComplexity` instead of `gocyclo.Stat` in `computeScores` test cases (including `makeStat` helper). Also update `lookupCoverage` tests in `internal/crap/crap_test.go` that reference `gocyclo.Stat`, and remove the `gocyclo` import from test files.

## 3. Quality Pipeline Provider

- [x] 3.1 Create `internal/provider/goprovider/sideeffect.go` with `GoSideEffectAnalyzer` struct implementing `crap.SideEffectAnalyzer`. The `Analyze` method calls `analysis.LoadAndAnalyze(pkgPath, analysisOpts)` then `classify.Classify(results, classifyOpts)`. Include config fields for all `classify.Options` dependencies (`*config.GazeConfig`, module packages `[]*packages.Package`, `TargetPkg *packages.Package`, verbose). Include a constructor accepting these dependencies.
- [x] 3.2a Create `internal/provider/goprovider/contract.go` with `GoContractCoverageProvider` struct and constructor. The struct includes fields for `Stderr io.Writer`, `AIMapperFunc`, and internal dependencies. The `Build` method signature matches `crap.ContractCoverageProvider`. Initial implementation: call the existing `BuildContractCoverageFunc` internally (thin wrapper). This ensures byte-identical behavior immediately.
- [x] 3.2b Verify `GoContractCoverageProvider.Build()` produces identical output to direct `BuildContractCoverageFunc` calls by running `go test -race -count=1 -short ./internal/crap/...`.
- [x] 3.3 Refactor `crap.Analyze` in `internal/crap/analyze.go` to use `ContractCoverageProvider` from `Options` when set. When `ContractCoverageProvider` is non-nil, call `Build()` and use the returned lookup function and degraded packages. When nil, fall back to the existing `ContractCoverageFunc`/`SSADegradedPackages` pattern. This ensures zero behavior change for callers that haven't migrated yet.
- [x] 3.4 Update callers of `BuildContractCoverageFunc` in `cmd/gaze/main.go` and `internal/aireport/runner.go` to construct `GoContractCoverageProvider` and pass it via `crap.Options.ContractCoverageProvider`. Keep `ContractCoverageFunc` fallback path intact for backward compatibility but do not use it in production callers. Run `TestSC002_GazeCRAPloadMatchBetweenCrapAndReport` to confirm no regression in the report pipeline.

## 4. Mock Providers

- [x] 4.1 Create `internal/provider/mockprovider/mock.go` with mock implementations: `MockComplexityProvider` (configurable `[]crap.FunctionComplexity` and optional error), `MockLineCoverageProvider` (configurable `[]crap.FuncCoverage` and optional error), `MockSideEffectAnalyzer` (configurable `[]taxonomy.AnalysisResult` and optional error), `MockContractCoverageProvider` (configurable lookup function, degraded packages, and optional error). Each mock stores its configured data and returns it on the corresponding method call. Use `io.Discard` for stderr in tests. Mock `FunctionComplexity` entries should use file paths to `testdata/` fixtures for `isGeneratedFile` compatibility.
- [x] 4.2 Create `internal/provider/mockprovider/mock_test.go` with tests: (1) Mock complexity + coverage providers produce CRAP scores matching `crap.Formula()` for 3 functions with known values. (2) All four mock providers produce a valid `crap.Report` with correct quadrant classifications and fix strategies for 5 functions spanning all four quadrants. (3) Edge case: empty provider results produce an empty report (no scores, zero-valued summary), not an error. (4) ContractCoverageProvider returning reason `"all_effects_ambiguous"` flows through to `Score.ContractCoverageReason`. (5) ContractCoverageProvider returning error causes graceful degradation (CRAP scores computed without GazeCRAP). (6) Provider precedence: when both `ContractCoverageProvider` and `ContractCoverageFunc` are set, verify `ContractCoverageProvider.Build()` is called and `ContractCoverageFunc` is never invoked.
- [x] 4.3 Create `internal/provider/goprovider/goprovider_test.go` with compile-time interface satisfaction checks: `var _ crap.ComplexityProvider = (*GoComplexityProvider)(nil)`, etc. for all four interfaces. These verify type compatibility. Behavioral correctness of Go adapters is verified by the E2E self-check (task 5.2) which exercises the full pipeline with Go providers.

## 5. Non-Regression Verification

- [x] 5.1 Run `go test -race -count=1 -short ./...` -- all tests MUST pass with zero failures.
- [x] 5.2 Run `go test -race -count=1 -run TestRunSelfCheck -timeout 30m ./cmd/gaze/...` -- E2E self-check MUST produce identical scores and report output. This exercises all four Go provider adapters through the production call path.
- [x] 5.3 Run `golangci-lint run` -- MUST pass with no new warnings. (golangci-lint not installed locally; CI will validate)
- [x] 5.4 Verify `internal/crap/provider.go` has zero imports from `go/ast`, `go/types`, `go/packages`, `go/ssa`, or `github.com/fzipp/gocyclo`.
- [x] 5.5 Run existing benchmarks (`go test -bench=. -benchmem ./internal/crap/...`) and verify no measurable performance regression from interface dispatch.

## 6. Documentation

- [x] 6.1 Update `AGENTS.md` -- add `internal/provider/goprovider/` to the architecture section. Add a "Key Patterns" entry for the provider interface pattern. Add to Recent Changes.
- [x] 6.2 Add GoDoc comments on all new exported types (including `FunctionComplexity`), interfaces, methods, and constructors in `provider.go`, `goprovider/*.go`, and `mockprovider/*.go`.

## 7. Constitution Alignment Verification

- [x] 7.1 Verify all four principles: Accuracy (byte-identical output with Go providers, all tests pass), Minimal Assumptions (no user-facing changes, no new CLI flags or config), Actionable Output (output formats unchanged, reports byte-identical), Testability (each interface independently testable via mocks, universal scoring core testable with synthetic data). Coverage targets: `provider.go` 100% (type definitions), `goprovider/` covered via E2E self-check + compile-time checks, `mockprovider/` 100% unit coverage via task 4.2.
<!-- spec-review: passed -->
<!-- code-review: passed -->